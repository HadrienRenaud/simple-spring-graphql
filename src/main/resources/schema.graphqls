type User {
    id: ID
    username: String
    email: String
}

enum MediaTypes {
    AUDIO
    VIDEO
    DISCO
}

type MediaFile {
    id: ID!
    type: MediaTypes!
    name: String!
    duration: String!
    addedBy: User!
    url: String!
}

type Query {
    medias: [MediaFile]
}
